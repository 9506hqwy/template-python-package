name: CI

on:
  push:
    branches: [ main ]
    tags-ignore: [ '**' ]
  pull_request:

permissions:
  contents: read

jobs:
  ubuntu:
    uses: 9506hqwy/actions/.github/workflows/python-ci-ubuntu.yml@main

  windows:
    uses: 9506hqwy/actions/.github/workflows/python-ci-windows.yml@main
